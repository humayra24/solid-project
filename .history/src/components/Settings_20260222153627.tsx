

type SettingsData = {
    noti
    
}

export default function Settings() {
    
    const [settings, setSettings] = createSignal(...defaultSettings);
    return (
        <main>
            
        </main>
    );
}